

var info = document.getElementById('info-field');
var info = document.getElementById('info-field');

var action = function(){

info.style.background = '#000000'

    info.removeEventListener('click', action);
}


var event = info.addEventListener('click', action);

